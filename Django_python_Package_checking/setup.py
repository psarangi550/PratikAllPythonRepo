#here we are trying to install the pattern package
from setuptools import *#importing all the files from the setuptools module
#here we need to use seup() to install the package
setup(name="multiplication",version="1.0",package=["pattern"])
